// Définition de l'interface Launchpad
export interface Launchpad {
    id: string;            // Identifiant unique du site de lancement
    full_name: string;     // Nom complet du site de lancement (par exemple, "Cape Canaveral Space Force Station")
}
